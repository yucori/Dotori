<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- 로그 파일 경로 -->
    <property name="LOG_PATH" value="${LOG_PATH:-./logs}"/>
    <property name="LOG_FILE" value="${LOG_FILE:-dotori}"/>

    <!-- 콘솔 출력 (동기) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 중요 로그 파일 (동기) - 인증, 에러 등 유실되면 안되는 로그 -->
    <!-- 동기적으로 직접 파일에 쓰기 (로그 유실 방지) -->
    <appender name="CRITICAL_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}-critical.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}-critical-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <!-- 즉시 플러시하여 로그 유실 방지 -->
        <immediateFlush>true</immediateFlush>
    </appender>

    <!-- 에러 로그 파일 (동기) - 유실되면 안되는 에러 로그 -->
    <!-- 동기적으로 직접 파일에 쓰기 (로그 유실 방지) -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}-error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <!-- 즉시 플러시하여 로그 유실 방지 -->
        <immediateFlush>true</immediateFlush>
    </appender>

    <!-- 일반 로그 파일 (비동기용) - 성능을 위해 비동기 사용 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 비동기 Appender - 일반 로그용 (성능 최적화) -->
    <!-- neverBlock=true: 버퍼가 가득 차면 로그를 버림 (성능 우선) -->
    <!-- neverBlock=false: 버퍼가 가득 차면 블로킹 (로그 유실 방지) -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>false</includeCallerData>
    </appender>

    <!-- 애플리케이션 로거 설정 -->
    
    <!-- 인증 관련 로거 - 동기 로거 사용 (중요한 로그, 유실 방지) -->
    <logger name="com.dotori.dotori.auth" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="CRITICAL_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </logger>

    <!-- 예외 핸들러 - 동기 로거 사용 (중요한 로그, 유실 방지) -->
    <logger name="com.dotori.dotori.global.exception" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="CRITICAL_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </logger>

    <!-- 보안 관련 로거 - 동기 로거 사용 (중요한 로그, 유실 방지) -->
    <logger name="com.dotori.dotori.auth.security" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="CRITICAL_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </logger>

    <!-- 일반 비즈니스 로직 로거 - 비동기 로거 사용 -->
    <logger name="com.dotori.dotori.task" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="com.dotori.dotori.user" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- Spring Security 로그 레벨 조정 -->
    <logger name="org.springframework.security" level="INFO"/>

    <!-- SQL 로그 (개발 환경에서만) -->
    <springProfile name="!prod">
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
    </springProfile>

    <!-- 루트 로거 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>

